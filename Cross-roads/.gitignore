# Local Terraform files
*.tfstate
*.tfstate.*
.terraform/
.terraform.lock.hcl
crash.log

# Optional: ignore sensitive config (only if you don't want to track these)
*.tfvars
*.tfvars.json

# Mac system files (optional)
.DS_Store
